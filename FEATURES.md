# 功能与兼容性边界

0.4 已补：shell 词法和有限命令分析、常见包管理器/EXPOSE/HEALTHCHECK/ENV 检查；ARG/ENV 阶段作用域 API；严格 JSON 配置、忽略/级别/镜像仓库策略；多文件和 SARIF/Checkstyle/GNU/JSON CLI。

189 个可比较官方 Hadolint 2.15.1 输入中 175 一致、14 项有意差异；3 项官方解析错误另列。这里只比较明确的 46 个 DL 编号在当前语料上的代码/行号/级别，不是这些规则所有分支已完善，更不是全部上游兼容。

仍需完成：

- 完整 ShellCheck AST/数据流/函数/数组/复合语法、全部包管理器选项与所有规则分支、条件路径和跨 RUN 缓存分析。
- 标签 schema/元数据规则、其余 Hadolint 规则与官方完整套件；现有参考语料还包含未实现的 SC 和 DL3066 输出。
- BuildKit 的完整语法/展开、自动平台 ARG、基础镜像元数据、ONBUILD 求值、扩展参数操作、Windows/CMD/PowerShell 与 escape 指令一致性。变量快照不等于实际构建结果。
- YAML/环境配置与自动发现、完整上游输出格式/精确消息和列号；浏览器还未提供变量/配置编辑界面。
- 大型真实 Dockerfile 语料、代表性同机性能/内存比较、跨平台和长期部署。当前基准仅示例计时，不支持性能追平结论。

限制：Dockerfile/shell 字符串最多 100 万 UTF-16 单元，shell token 最多 10 万，阶段最多 256；命令替换深度最多 32，词法嵌套最多 128。变量展开最多 100 万单元/32 层，快照最多 1 万条并有约 8M 累计变量单元预算。CLI 每个输入/配置最多 2 MiB、总读取最多 16 MiB、文件最多 256。未知基础镜像变量保留为 unresolved，不拉取镜像。

保留独立仓库及本地构建/测试/证据，未上传。仍未完成全面追平。
