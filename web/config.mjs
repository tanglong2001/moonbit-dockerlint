export default {"slug": "dockerlint", "title": "Dockerfile 解析与检查", "description": "带行号和多阶段上下文的 Dockerfile 静态分析", "source": "https://github.com/hadolint/hadolint", "scope": "逻辑行、指令、FROM 阶段和常见安全/可复现性诊断", "limitations": "完整 BuildKit heredoc、ShellCheck、完整 hadolint 规则集", "version": "0.1.0", "example": "FROM ubuntu:latest\nUSER root\nRUN curl https://example.com | sh\nCMD echo ready\n"};
