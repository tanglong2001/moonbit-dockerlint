export default {"slug": "dockerlint", "title": "Dockerfile 解析与检查", "description": "带行号和多阶段上下文的 Dockerfile 静态分析", "source": "https://github.com/hadolint/hadolint", "scope": "逻辑行、阶段依赖、RUN 命令与包管理器检查；另提供变量作用域、配置和多格式 CLI", "limitations": "未实现完整 ShellCheck、BuildKit/Windows 语义；官方对照含已记录差异，尚未追平", "version": "0.5.0", "example": "FROM ubuntu:latest\nUSER root\nRUN curl https://example.com | sh\nCMD echo ready\n"};
