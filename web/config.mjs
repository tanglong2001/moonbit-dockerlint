export default {"slug": "dockerlint", "title": "Dockerfile 解析与检查", "description": "带行号和多阶段上下文的 Dockerfile 静态分析", "source": "https://github.com/hadolint/hadolint", "scope": "逻辑行、指令、FROM 阶段和常见安全/可复现性诊断；增加一站式解析检查与规则忽略 API，修复非法 UTF-16 崩溃", "limitations": "规则数量和 shell 分析远少于 hadolint，未提供完整 Dockerfile 语义", "version": "0.2.0", "example": "FROM ubuntu:latest\nUSER root\nRUN curl https://example.com | sh\nCMD echo ready\n"};
